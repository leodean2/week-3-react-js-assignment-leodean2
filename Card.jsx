     const Card = ({ title, content }) => (
       <div className="border rounded p-4 shadow">
         <h2 className="font-bold">{title}</h2>
         <p>{content}</p>
       </div>
     );
     
     export default Card;
     